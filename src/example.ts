//!This code will not be compiled by default as it is not included.
// feel free to use this example as some starter code 😀

import { Component, selectMainComponent } from "./index" // replace ./index with swore

let app = new Component()
selectMainComponent(app) // this selects the main component and adds auto rendering